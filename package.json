{
  "name": "contra-alerts",
  "version": "1.0.0",
  "description": "Minimal Playwright scraper for Contra job board with GitHub Actions scheduling and notifications",
  "type": "module",
  "scripts": {
    "start": "tsx scripts/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "resend": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
